name: healthoffice
description: "Health and Environment Office Portal – Capital Secretariat, Sana'a"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # State Management
  provider: ^6.1.2
  # HTTP & API
  http: ^1.2.1
  # Routing
  go_router: ^14.8.1
  # Storage
  shared_preferences: ^2.3.4
  # Intl  
  intl: ^0.20.2
  # Icons
  font_awesome_flutter: ^10.8.0
  # Image Picker
  image_picker: ^1.1.2
  # File Picker
  file_picker: ^8.1.7
  # Google Fonts
  google_fonts: ^6.2.1
  # Animations
  flutter_animate: ^4.5.2
  # Cached images
  cached_network_image: ^3.4.1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/lang/
